
import React from 'react';
import { StateInfoProps } from './types';

export const StateInfo: React.FC<StateInfoProps> = ({
  currentSquatState,
  poseScore
}) => {
  // This component is used to organize the drawing logic for state information
  return null;
};
