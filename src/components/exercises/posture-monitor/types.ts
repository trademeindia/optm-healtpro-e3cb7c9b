
// This is a new file where we define the FeedbackType enum
export enum FeedbackType {
  SUCCESS = "success",
  ERROR = "error",
  WARNING = "warning",
  INFO = "info",
}

// Add other necessary types if needed
