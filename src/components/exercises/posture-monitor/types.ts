
// This is a new file where we define the FeedbackType enum
export enum FeedbackType {
  SUCCESS = "success",
  ERROR = "error",
  WARNING = "warning",
  INFO = "info",
}

// Add the SquatState enum that's being imported but not exported
export enum SquatState {
  STANDING = "standing",
  MID_SQUAT = "mid_squat",
  BOTTOM_SQUAT = "bottom_squat"
}

// Add other necessary types if needed
