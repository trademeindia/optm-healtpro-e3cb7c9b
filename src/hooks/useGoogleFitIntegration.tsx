
// We're now importing from the new directory structure
import { useGoogleFitIntegration, GoogleFitIntegrationReturn } from './google-fit';

// Re-export the hook and its return type
export { useGoogleFitIntegration };
export type { GoogleFitIntegrationReturn };
